## URL of the deployed Colyseus server (WebSocket).
## Local dev: leave unset — the client falls back to ws://<hostname>:2567.
## Prod: set to wss://your-server-host
# VITE_SERVER_URL=wss://your-server-host
